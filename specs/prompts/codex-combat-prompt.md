# Codex Combat Prompt

Read `AGENTS.md`, `specs/08-combat-spec.md`, `specs/09-entity-spec.md`, and `specs/15-testing-and-acceptance.md`.

Implement:

- one blaster weapon
- visible projectile firing
- projectile lifetime cleanup
- simple destructible targets
- score increments on destroy
- minimal hit feedback hooks

Keep the implementation MVP-sized and avoid introducing a full ECS.
